# Port : ignoré par Render (il injecte PORT), utile en local
PORT=4000

# CORS
ALLOWED_ORIGIN=*

# Fournisseur Mobile Money (mock pour tests)
MOBILE_MONEY_PROVIDER=mock

# Clés Stripe (à mettre dans Render en variables d'environnement, pas en clair ici)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# URL publique du serveur (optionnel, utile côté app si tu veux la relire)
PUBLIC_BASE_URL=https://<ton-service>.onrender.com
